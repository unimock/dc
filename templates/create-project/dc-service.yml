version: "1"
service:
#  project_name: <enter a unique project name, if you can not use COMPOSE_PROJECT_NAME in .env
#  config_check: false  # set to false, if the service don not fit into the dc environment. This will not cover all dc commands.
  state: 
    - active
  # enter the main service name of the compose project to jump into the system with dc login or to copy files.
  name: <main_service>
#  group:
#    - 
  control:
    type: compose
    file: docker-compose.yml
  hosts:
    - <host>
